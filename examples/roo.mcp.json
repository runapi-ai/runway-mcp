{
  "mcpServers": {
    "runway": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/runway-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
